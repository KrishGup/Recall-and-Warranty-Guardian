# read_this_labubu.md

**Where things are (three lines):**

1. Guardian runs end to end on gren + Strands: `guardian seed` → `guardian serve` → click the logo (or `guardian sweep`) pulls live CPSC/NHTSA/openFDA recalls, matches them in code, adjudicates the ambiguous pairs with Opus, verifies the plan, pauses on the household gate, and after "Request full refund" in the dashboard drafts and "sends" the remedy (to `var/household/outbox` until SES/SNS are configured), then schedules the follow-up. Verified live on 2026-09-13 with real recalls (Boon NURSH bottles, Cade finger lights, Hampton Bay fan, 2019 Outback campaigns); 32 pytest tests cover it on the mock provider, including snoozes and late approvals (an approval releases the gate at once; a later one forks the run from the gate).
2. The web app (`web/`, Vite + React) implements the whole design handoff: the six dashboard pages with the shell, panels, dark/RTL/mobile, and `/flow/trace`, the full gren trace view (runs, canvas, drawer, inspector, gate approval) live over SSE; `npm run build` and `guardian serve` host it at http://127.0.0.1:8787, `VITE_MOCK=1 npm run dev` runs it with no backend.
3. AWS: the account (779457758734, us-east-1) is active and Guardian is live at **https://guardian.44-214-230-44.sslip.io** (one EC2 instance + Caddy TLS, built by `scripts/deploy_ec2.py`; `update` pushes new code in about a minute, `logs`/`run` go through SSM, the store mirrors to the S3 state bucket, a systemd timer runs the sweep at 06:00 UTC). Reads are open; actions need `GUARDIAN_API_TOKEN` from `.env` (Krish has it; open `<url>/?token=<token>` once). One gate remains on the AWS side: Bedrock is not yet authorized for this account at all (every model, every region answers `Operation not allowed`, and even the Anthropic use-case form says "create a support case"), so the live server shows the household and pulls the feeds but cannot finish a sweep until AWS Support authorizes Bedrock (case 178934068800881, opened 2026-09-13 19:04 EDT, answer promised within 24 h; see `docs/AWS_SETUP.md` section 3). Fallback for the video if that is slow: `ANTHROPIC_API_KEY` in `.env` runs the same graphs on the Anthropic API, and `GUARDIAN_DEPLOY_BRIDGE=anthropic` + `deploy_ec2.py update` puts the server on it. `deploy/agentcore` is the AgentCore path; it deploys only after AWS Support raises the account's AgentCore quotas from 0 (support case, not filed yet). Still not built: SES/SNS delivery beyond the outbox, the demo video, `docs/architecture.png`, and the plan's Expanded tier (mailroom photos, grocery advisories, settlements).

Everything else you need is in `README.md` (quickstart, node table), `ARCHITECTURE.md` (how the graphs sit on Strands, where AWS attaches), `CLAUDE.md` (repo conventions for Claude sessions), and gren's own `gren/docs/HANDOFF.md`.
