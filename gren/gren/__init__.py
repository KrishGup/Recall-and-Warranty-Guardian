"""gren 2 - Graph Engineering Runtime built on Strands Agents.

The graph-engineering layer (spec, dependency test, reducers, adversarial verifiers with kill authority,
human gates, bounded loops, budgets, graph-shaped metrics) is gren's. The harness underneath - agent loop,
model providers, tools, structured output, hooks, telemetry, session persistence, interrupts and the
multi-agent Graph executor - is Strands Agents.
"""

__version__ = "2.0.0"
